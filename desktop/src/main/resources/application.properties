# Backend REST API base URL. Matches the backend's default port (8081 - 8080 is
# avoided since it commonly collides with other local dev servers). Override
# with -Dapi.base-url=http://host:port on the java command line if needed.
api.base-url=http://localhost:8081
