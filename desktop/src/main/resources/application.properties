# Backend REST API base URL. Override with -Dapi.base-url=http://host:port when launching
# (e.g. mvn javafx:run -Djavafx.run.jvmArgs="-Dapi.base-url=http://localhost:8080") so the
# desktop client is never hard-coded to one environment.
api.base-url=http://localhost:8080
