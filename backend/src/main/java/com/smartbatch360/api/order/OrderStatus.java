package com.smartbatch360.api.order;

/**
 * Order lifecycle state. Only UNFULFILLED exists for now - the rest of the
 * lifecycle is deliberately not built yet (user scope decision, 2026-08-27).
 */
public enum OrderStatus {
    UNFULFILLED
}
